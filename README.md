hola profesor, los archivos php, son para cuando trabajamos administrador con base de datos, lo hicimos porque no sabiamos que teniamos que mostrar el proyecto sin base ni localhost
para probar el programa solo debe abrir el Index html, y para ver el administrador tiene que abrir el archivo admin.html
